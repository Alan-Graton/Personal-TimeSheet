import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
    tableContainer: {
        borderRadius: 15,
        margin: "10px 270px",
        maxWidth: 950,
        marginTop: "70px",
    },
    tableRoot: {
        width: "100%",
    },
})

export default useStyles;